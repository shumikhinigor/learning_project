import React from 'react';
import { Container } from '@mantine/core';

import { Layout } from 'components/ui';

export const Profile = () => {
    return (
        <Layout>
            <Container py={24}>Profile</Container>
        </Layout>
    );
};
