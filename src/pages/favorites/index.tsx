import React from 'react';
import { Container } from '@mantine/core';

import { Layout } from 'components/ui';

export const Favorites = () => {
    return (
        <Layout>
            <Container py={24}>Favorites</Container>
        </Layout>
    );
};
